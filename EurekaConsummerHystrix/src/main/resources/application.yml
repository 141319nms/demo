#security:
  #basic:
    #enabled: true
  #user:
    #name: user
    #password: password123
    
server:
  port: 9002 #default,customizable
  
spring:
  application:
    name: EurekaConsummerHystrix  #customizable ,the registry-center has not
  
eureka:
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka
      
logging:
  level:
    root: INFO